declare class AsyncQueue<T> {
    private queue;
    private resolvers;
    push(item: T): void;
    pop(): Promise<T>;
    isEmpty(): boolean;
    clear(): void;
}
declare class AsyncEvent {
    private flag;
    private resolvers;
    set(): void;
    clear(): void;
    wait(): Promise<void>;
}
export { AsyncQueue, AsyncEvent };
